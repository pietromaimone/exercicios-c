/*3. Escreva um programa que receba a velocidade máxima em uma avenida e a
velocidade com que um motorista estava dirigindo nela. Calcule a multa que o
motorista vai receber, considerando que são pagos R$5,00 por cada km/h que
estiver acima da velocidade permitida (considere apenas números inteiros). Se
a velocidade do motorista estiver dentro do limite, o programa deve informar
que não há multa.*/


#include <stdio.h>

int main() {
    
    int velocidademotorista , velocidademax , multa , excesso;
    
    printf ("Digite a velocidade maxima da avenida (km/h):");
    scanf ("%d",&velocidademax);
    
     printf("Digite a velocidade do motorista (km/h): ");
    scanf("%d", &velocidademotorista);
    
    if( velocidademotorista > velocidademax){
        excesso = velocidademotorista - velocidademax;
        multa = excesso * 5 ;
        
    printf("O motorista excedeu o limite em %d km/h.\n", excesso);   
    printf ("o valor da multa e %d\n", multa );
    
    
    }
    else
        printf("voce nao esta multado");
    
    
    
    
    return 0;
}
